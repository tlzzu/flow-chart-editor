export default {
  name: "addNode", //新增node节点命令
  /**
   * 执行命令
   * @param {Object} node node节点
   */
  exec(node) {
    //todo 这里执行新增，this为当前整个对象
  },
  /**
   * 查询命令
   */
  query() {
    //todo
  }
};